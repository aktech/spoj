#include<stdio.h>
#include<math.h>
int main()
{
	int cases,n,Tcases,k,i;
	scanf("%d",&Tcases);
	for(cases=0; cases<Tcases; cases++)
	{   k = 0;
	    i=1;
		scanf("%d",&n);
		while(n >= pow(5,i))
		{
			k = k + (n)/(pow(5,i));
			i++;
			}
			printf("%d\n", k);
		}
		return 0;
}
